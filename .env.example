# TrainerPro Admin Web - Environment Variables (EXAMPLE)
# Copy to .env and fill with real values
# NEVER commit .env with real values!

# DATABASE (REQUIRED)
# PostgreSQL connection URL (Supabase or other provider)
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE"

# NEXT AUTH (REQUIRED)
# Base URL of the application
# Development: http://localhost:3000
# Production: https://admin.trainerpro.com
NEXTAUTH_URL="https://admin.trainerpro.com"

# NEXT AUTH SECRET (REQUIRED)
# Generate with: openssl rand -base64 32
# NEVER use example value in production!
NEXTAUTH_SECRET="CHANGE_THIS_TO_A_RANDOM_SECRET"

# NODE_ENV (Managed by Vercel)
# Vercel automatically sets this to "production" in production
# Only set manually if deploying to other platforms
NODE_ENV="production"
